# Project state

- Current sprint: Fast-track MVP vertical slice, plus the authorized next product phase (daily/weekly summaries, `#posting` checklist, recurring tasks, workload/bottleneck analytics)
- Current branch: `fast-track/mvp`
- Current objective: Live-verify the next-phase features just deployed (morning/evening/weekly Telegram summaries, `#posting` checklist gate, recurring task generation, Reports tab) against production, using the manual cron trigger steps in the latest report; the older MVP live-verification backlog below (ordinary-conversation non-trigger, deadline-request/review round trips, Mini App click-through) is still open and can be resumed alongside it.
- Completed sprints: Sprint 0 complete and independently reviewed.
- Completed current tasks:
  - Product, architecture, sprint, and Sprint 0 documentation
  - Next.js/TypeScript application skeleton
  - Foundational Supabase schema and RLS lock-down
  - Shared permission and event foundations
  - Telegram webhook and Mini App shell
  - Foundation test/build gate and fast-track backend gate
  - Claude general and security reviews with no Critical/High findings
  - Revision-history immutability and webhook/config hardening
  - P3 mobile-first Mini App: My Tasks, Team, Today, Overdue, Kanban, detail, Quick Add, workflow actions, deadline requests, and Head activation
  - Secure configured-Head bootstrap and configured marketing-group enforcement
  - All five ordered migrations applied to the live Supabase project; service-role access succeeds and browser roles remain denied
  - Telegram username ownership is kept unambiguous across renames/reuse
  - Forged Mini App authentication returns a sanitized 401; Head My Tasks and Team scopes are distinct
  - Production Vercel deployment with encrypted runtime configuration and stable `APP_URL`
  - Telegram webhook and Mini App menu configured to the production origin with no pending/error state
  - Real `/start` delivery verified through Head onboarding and bot reply; configured group and private chat are reachable
  - Controlled production task smoke verified group restriction, task persistence, group/assignee delivery, signed Mini App authentication, deadline approval, full lifecycle to `DONE`, audit events, and revision history
  - Group shorthand now accepts standalone/inline bot mentions, blank lines, ordinary prefix whitespace, and case-insensitive fields; incomplete shorthand gets a useful format response
  - Telegram webhook diagnostics record safe update structure and group parse/create outcomes without message bodies, identities, or secrets
  - Telegram and Mini App task creation now share one post-persistence assignment-notification workflow with explicit sent/failed outcomes
  - Mini App Quick Add reports notification success/failure, and notification failure preserves the already-created task and immutable `TASK_CREATED` audit event
  - Focused fix commit `63d12c6` is pushed and deployed to the stable production URL; health, webhook, menu, production environment names, and Supabase counts were reverified
  - Production onboarding count remains exactly 3 persisted users with real Telegram IDs (all active); 2 of the expected 5 teammates are not yet onboarded and no placeholder identities were created
  - Real human marketing-group update `4315790` reached production as a group message with a Telegram mention entity, created exactly one task, persisted `TASK_CREATED`, and logged assignment notification `SENT`
  - The notification's real Accept callback `4315791` reached production; the smoke task is `IN_PROGRESS` with `TASK_CREATED`, `STATUS_CHANGED`, and `TASK_ACCEPTED` history
  - Shared application workflows now await Telegram notifications for deadline requests/decisions, review submissions, approvals, revisions, and blocked tasks
  - Workflow recipients include creator plus active Head where required and are deduplicated by Telegram user ID; missing onboarding and delivery failures preserve committed workflow state and emit safe structured outcomes
  - Telegram deadline approval/rejection and review approval/revision buttons route through the same application services used by Mini App APIs
  - Independent General and Opus security reviews pass with no Critical/High findings; findings are recorded in `.ai/reviews/20260907-workflow-notification-triage.md`
  - Telegram group task creation now treats the no-mention positional title / `@assignee` / deadline format as primary while retaining labeled and mention-based compatibility
  - Positional classification ignores normal group conversation before actor lookup; attempted tasks require an active sender, active onboarded assignee, valid future Tashkent deadline, and use the existing audited notification-aware creation service
  - Final Opus security review of the complete combined diff (notification fixes + positional syntax) passed `PASS_WITH_NOTES` with no Critical/High findings after Opus quota reset; both Low findings (inactive-recipient notification leakage; an unanchored deadline-hint regex that could fire the classifier on a bare clock-time mention in ordinary conversation) were fixed and covered by new regression tests before this pass
  - Commits `77b2c4b` (fix) and `729e379` (docs/review artifacts) pushed to `origin/fast-track/mvp` and deployed to production (Vercel deployment `dpl_7VWZ1zijsex3D4EchY2dD8CSjUws`)
  - Post-deploy checks passed: `/api/health` returns 200; Telegram webhook still points at the production origin with zero pending updates and no last error; Mini App menu button still resolves to the production origin; Supabase REST is reachable with the service-role key
  - **Real human positional group task creation verified in production**: update `4315796` ("Yangi creative PROD POSITIONAL TEST" / `@misbahmarketing` / `08.09.2026 18:00` / `high` / description, no bot mention, no `T:/A:/DL:`) → positional classifier activated → sender `diyorbek_samadov` (HEAD_OF_MARKETING, active) authenticated → assignee `misbahmarketing` (CONTENT_MARKETER, active) resolved → exactly one task persisted (`74360fff-f0d5-4c41-9ba1-2c03cdc7a77b`, all fields matching the sent message) → exactly one `TASK_CREATED` audit event → group confirmation logged → private assignment notification `SENT`. Full chain traced via `vercel logs` and direct Supabase REST queries.
  - Practical full-MVP audit completed (`docs/MVP_AUDIT.md`): no P0 findings; P1s (unstyled default-priority pill, missing Home dashboard) fixed; several P2 cleanups fixed (dead CSS, unreachable Review/Blocked filters, stale Home data after mutation, generic empty/error copy, mismatched theme color).
  - Mini App redesigned to a blue SaaS visual system (`#2563EB`/`#0039B7`, Inter, 14–18px radii) with a new Home tab (stat tiles, Head team workload, priority tasks), a Team tab, and bottom navigation; no backend/API changes. Committed `2b37acc`, pushed, and deployed to production; post-deploy health/webhook/menu/Supabase checks re-passed.
  - Accept-notification and role-editing product changes (commit `7ffbb29`): `TASK_ACCEPTED` workflow notification to the task creator from the one shared `performTaskAction` method (both Telegram and Mini App transports), and `MarketingService.updateUserRole` (Head corrects anyone's non-Head role, a member corrects their own, Head role never settable here) with a `POST /api/users/[id]/role` endpoint and a Mini App profile-sheet/Team-tab "Edit role" UI. See `docs/MVP_AUDIT.md` "Update — 2026-09-09: accept notifications + role editing".
  - **Next product phase (daily/weekly summaries, `#posting` checklist, recurring tasks, workload/bottleneck analytics)** — user-authorized reordering of the roadmap, implemented as one increment on `fast-track/mvp` with the Telegram positional task format left untouched:
    - One new migration (`20260909090000_next_product_phase.sql`): `recurring_definitions`, `task_checklists`, `task_checklist_items`, `report_deliveries` tables (RLS-enabled, `service_role`-only grants); `create_task_with_event_v2` (idempotent create, auto-attaches a posting checklist by deterministic `#posting` tag detection, tags recurring-generated tasks as `SYSTEM` actor); `transition_task_with_events` now also blocks `REVIEW` in-transaction when a posting checklist is incomplete; `toggle_posting_checklist_item` (assignee-only, `IN_PROGRESS`/`REVISION` only, audited); `generate_recurring_task` (row-locked, re-validates `next_occurrence_at` before creating, so concurrent/duplicate cron runs cannot double-create); a backfill block attaches checklists to pre-existing tagged tasks.
    - Protected Vercel Cron (`vercel.json`): daily summaries at 03:00/14:00 UTC (08:00/19:00 Asia/Tashkent, no DST), weekly at 05:00 UTC Sunday (10:00 Tashkent), recurring-generation sweep every 5 minutes; `GET /api/cron/[job]` validates `Authorization: Bearer $CRON_SECRET` with `timingSafeEqual`, 401 on mismatch, 503 if unconfigured.
    - `ReportingService` composes the exact morning/evening/weekly message formats specified (Uzbek employee copy, Head team rollups with top-3-plus-totals sections) from deterministic metrics in `src/domain/reporting.ts`; every recipient delivery is claimed against `report_deliveries` first (unique `(report_type, interval_key, recipient_user_id)`) so a duplicate cron invocation cannot double-send; never-`/start`'d users are excluded from delivery (not fabricated), and how many were excluded is now logged.
    - `#posting` checklist: exact, case-insensitive tag anywhere in title/description (word-boundary matched so `#postings` does not match) attaches a persistent 4-item checklist (Telegram/Instagram/YouTube/X-Twitter); Telegram shows an inline toggle keyboard when `SUBMIT_REVIEW`/`/review` is attempted incomplete (no typing required); Mini App Task Detail shows a compact checklist card, toggleable only by the assignee while `IN_PROGRESS`/`REVISION`; the DB rejects `REVIEW` independently of the app-layer check.
    - Recurring tasks: Mini App Task Detail "Make recurring" (Mon–Fri / weekly-on-weekday / monthly-on-day, local time, optional inclusive end date; day 31 clamps to the month's last day); pause/resume/edit/stop change only future occurrences; Head manages any recurrence, the creator manages their own, a non-owning assignee is blocked; every generated occurrence is a normal `tasks` row created through the same `createTask`-equivalent DB function (so audit, permissions, and the Telegram assignment notification all just work), tagged with `recurring_definition_id`/`scheduled_occurrence_at` for traceability.
    - Analytics: `GET /api/reports` (`getAnalytics`) — 30-day-default factual counts (created/completed/on-time/carry-over/current-overdue/revision-rate/deadline-change-requests/avg-cycle/avg-time-per-status/avg-blocked/avg-review-wait) plus `workloadMetrics` (open/in-progress/high/today/next-48h/overdue/blocked/review); Head sees a team breakdown, an employee sees only their own; no fabricated utilization/workload score anywhere. New Mini App "Reports" tab (compact cards, bars, status chips — no giant tables).
    - Practical audit performed on the already-drafted implementation (found largely complete, not built from scratch this pass): confirmed cron auth, DB-level idempotency (including true-concurrency, not just sequential-retry), and the Telegram checklist UX were already correct; fixed two observability gaps (recurring-task notification outcomes and never-onboarded report recipients were not logged); documented one cosmetic API-shape note (`/api/tasks/[id]/recurrence`'s `[id]` means task-id for `POST` but definition-id for `PATCH` — functionally correct, not renamed to avoid churn). Closed test-coverage gaps: complete-checklist-allows-review, duplicate-scheduler-run-produces-no-duplicate, `updateRecurrence` permission/pause/stop/resume-after-stop, `getAnalytics` employee-vs-Head visibility, weekly carry-over, and average blocked/review duration. See `docs/MVP_AUDIT.md` "Update — 2026-09-09: next product phase" for the full table.
    - Docs updated: `docs/PRODUCT.md` (metric definitions, posting/recurring sections, analytics description), `docs/ARCHITECTURE.md` (cron/notification/scheduling section, new data structures), `docs/SPRINTS.md` (new authorized-scope entry).
- Remaining tasks:
  - Confirm a real three-line non-task group message (no `@assignee`-shaped line 2) creates no task and draws no bot reply (message text was handed to the user; sending it was interrupted by this session's redirect to the audit/redesign work — retry next session)
  - Run a real deadline-change-request round trip (request → notify creator/Head → Approve → notify requester) and inspect Supabase state/audit
  - Run a real review round trip (SUBMIT_REVIEW → notify creator/Head → REQUEST_REVISION → notify assignee → SUBMIT_REVIEW again → APPROVE → notify assignee) and inspect audit history
  - Run the real Mini App Quick Add test against the deployed backend (now the redesigned UI) and verify the private assignment notification
  - Interactive visual QA of the redesigned Mini App from a real Telegram session (this session could only SSR-smoke-test; no Telegram session or headless browser was available to click through Home/Tasks/Team/Kanban/Detail/Quick Add as a user)
  - P5 deadline reminders remain optional/deferred and are not required for the fast-track usable MVP
  - **New, from the next-product-phase pass**: real Telegram verification of the 08:00/19:00 daily summaries, the Sunday 10:00 weekly summary, a live `#posting` task through the full checklist→REVIEW gate, a live recurring-task occurrence generated by the cron sweep, and a click-through of the new Mini App Reports tab. A safe manual trigger for the report/recurring cron logic (same `Authorization: Bearer $CRON_SECRET` gate, just invoked on demand instead of waiting for the schedule) is documented in the handoff report rather than left as a public endpoint.
  - Apply migration `20260909090000_next_product_phase.sql` to the live Supabase project (was pending at the time this pass started; see the deployment result immediately below for whether it was applied this session).
  - Set `CRON_SECRET` in the production Vercel environment if not already present, and confirm Vercel's Cron schedules in `vercel.json` are picked up by the linked project (Vercel Cron requires a `vercel.json` deploy and, on some plans, a Pro plan for more than 2 cron jobs — verify this project's plan supports the 4 jobs configured here).
- Deferred roadmap items: advanced project management, calendar/Google integrations, effort points, time tracking, internal chat, salary/KPI features, gamification, and a generalized workflow engine (all explicitly excluded by `docs/PRODUCT.md`). Deferred audit item: splitting `MiniApp.tsx` (now ~1980 lines post-next-phase) into multiple files — still no concrete maintainability problem surfaced, but it is getting large; worth a look next time UI work touches this file. `/api/tasks/[id]/recurrence`'s dual-meaning `[id]` param (see `docs/MVP_AUDIT.md` J5) is a documented-but-not-fixed cosmetic API-shape note.
- Latest test/build state: 2026-09-09 — lint PASS; typecheck PASS; 129/129 tests PASS (19 files); Next.js production build PASS (17 routes, 4 new: `/api/cron/[job]`, `/api/reports`, `/api/tasks/[id]/recurrence`, `/api/checklist-items/[id]/toggle`); npm production audit (prod deps) reports 0 vulnerabilities; `git diff --check` clean.
- Latest Claude review verdict: no fresh multi-agent security review was run this pass (out of scope per the user's instructions to focus on practical product quality); a full manual read-through of the new migration, service, repository, route, and UI code was performed instead (see `docs/MVP_AUDIT.md` "Update — 2026-09-09: next product phase" for what was checked and what was fixed).
- Unresolved findings: None blocking. See `docs/MVP_AUDIT.md` J5 (documented, not fixed) for the one deferred cosmetic item.
- Known blockers: None. The next-phase live-verification checks listed above require real Telegram time-of-day/day-of-week triggers or the documented manual-trigger workaround and could not be completed from this session; they are `PENDING_LIVE_VERIFICATION`.
- Latest commits: see `git log --oneline -5` on `fast-track/mvp` for the exact hash of this pass's commit (recorded here would go stale immediately since this file was updated in the same commit) — it follows `7ffbb29 feat: notify creator on task accept, let roles be corrected later`.
- Remote state: pushed to `origin/fast-track/mvp` and deployed to production — see the deployment result in the handoff report for the exact commit/deployment id.

## Live release state

- Production URL: `https://marketing-team-task-bot.vercel.app`
- Supabase: the next-phase migration (`20260909090000_next_product_phase.sql`) was pending against the linked remote project at the start of this pass — see the handoff report for whether it was applied and verified this session.
- Telegram: webhook/menu health from the prior pass was last verified 2026-09-07/08; re-verify after this deploy per the "Exact real tests still needed" list in the handoff report.
- Mini App: gains a Reports tab, a posting-checklist card, and a recurring-schedule card in Task Detail on top of the existing Home/Tasks/Team/Kanban/Detail/Quick Add screens; no existing screen was removed or redesigned again.
- Cron: four Vercel Cron jobs defined in `vercel.json` (daily morning/evening, weekly, recurring sweep every 5 minutes); requires `CRON_SECRET` set in the production environment to authorize (see Remaining tasks above).
